# Supabase project URL, e.g. https://xxxxxxxx.supabase.co (Project Settings → API)
SUPABASE_URL=
# Supabase SERVICE ROLE key — server-only secret, NEVER expose to the client (Project Settings → API)
SUPABASE_SERVICE_ROLE_KEY=
